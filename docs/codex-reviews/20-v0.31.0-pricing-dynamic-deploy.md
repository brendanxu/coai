# v0.31.0-pricing-dynamic — PROD DEPLOY ✅

**Deployed**: 2026-05-16 00:04 SGT (~4.5h after v0.30.0)
**Image**: `greentokey-coai:v0.31.0-pricing-dynamic` (78.7 MB, +0.2MB vs v0.30.0)
**Branch HEAD**: `ec659f9` (tag commit on top of `3d8a1f2`)
**Previous prod**: `v0.30.0-phase-1-excision` (rollback target)

## What shipped (cumulative since v0.30.0)

**Phase 2 ADMIN-L2-1 LOW-2 fold-in + PKG-PRICING-DYNAMIC** (6 commits, +870/-241 LOC):
- `d41fffa` gtk_provider_pricing +7 display columns (idempotent ALTER, MySQL+SQLite)
- `cb2b3df` public GET /gtk/v1/pricing endpoint (no auth, leak-free)
- `aa94b06` Pricing.tsx fetch /gtk/v1/pricing replacing hardcoded MODEL_ROWS
- `c9d4fb0` admin PricingTab + handler accept display_* fields (all-or-nothing toggle)
- `59b569b` UpdatePlanAPI is_active toggle audit log (ADMIN-L2-1 LOW-2 fold-in)
- `2ec5457` TestSeedProviderPricing_OnlyOnce update for display seed

**PKG-INDEX-TOOLBAR-EXCISE + PKG-I18N-CONFLICT-CLEANUP** (3 commits, -273 LOC):
- `1ff8d24` delete CoAI ToolBar from Index.tsx + dead icon imports
- `e3d965e` drop .toolbar CSS + .main left-reservation
- `3d8a1f2` resolve ja/ru conflict markers + drop unused React import (build unblock)

**Deploy infrastructure**:
- `ec659f9` bump tags v0.30.0-phase-1-excision → v0.31.0-pricing-dynamic

**Total**: 10 commits / net +596 LOC.

## Deploy steps executed

1. ✅ **Preflight** — cross-grep introduce-and-forget defender clean.
2. ✅ **rsync** — backup `coai-source.bak.v1.0.0004`; rsync Mac → VPS clean; verify keystone files present.
3. ✅ **Docker build** — Dockerfile.split Go-only, 4.2s, image v0.31.0-pricing-dynamic 78.7MB.
4. ✅ **Compose up** — `docker-compose.yml.bak.v1.0.0004` backup; image bumped; container recreated; boot healthy <10s.
   - Logs clean: mysql + redis connected, 4 users found, billing cron, SEO/PWA, static files serving.
   - **0 migration panics** — gtk_provider_pricing ALTER ran idempotent + seedDisplayPricing inserted/updated 8 display rows.
5. ✅ **Smoke tests** (6/6 PASS):
   - /token-plans bundle CTA i18n keys ✓
   - /api/payment/checkout HTTP 200 ✓
   - /api/payment/hupijiao/checkout HTTP 200 ✓
   - Hupijiao callback HTTP 500 (expected) ✓
   - /api/gtk/v1/services + pool both 200 ✓
   - token-99 plan row in gtk_plan ✓
6. ✅ **PKG-PRICING-DYNAMIC validation**:
   - `GET /api/gtk/v1/pricing` returns **8 models** (Claude 3.5 Sonnet, GPT-4o, Qwen2.5-Max, DeepSeek V3/R1, +3 more) — seedDisplayPricing migration baseline rows populated.
   - Sample row: `{"model":"Claude 3.5 Sonnet","vendor":"anthropic","context":"200k","priceIn":"21.60","priceOut":"108.00","creditsPerMOut":"5400","cache":"cache_control"}` — byte-equivalent to old Pricing.tsx hardcoded ✓.
   - Response shape matches frontend ModelRow exactly ✓.
   - No internal field leaks (no upstream_per_m / notes / effective_from in response) ✓.
7. ✅ **ADMIN-L2-1 endpoint gates**:
   - /admin/plans, /admin/billing-config, /admin/provider-pricing all return 200 + `{"error":"user not found"}` (auth-gate working).

## Pre-deploy fixes caught + landed

1. **ja/ru i18n conflict markers** blocking vite build (since v0.21 cleanup 2026-05-12). Resolved in `3d8a1f2` by dropping the conflict regions; vite [i18n] auto-translate plugin re-populated 689 keys × 100% coverage.
2. **Unused React import in Index.tsx** after ToolBar excise (TS6133). Fixed in same commit.

## Money-flow integrity

`git diff v0.30.0-phase-1-excision..v0.31.0-pricing-dynamic -- payment/ commerce/ service/ newapi/usage.go` shows **zero changes** in money-execution paths. ADMIN-L2-1 LOW-2 audit log only emits when is_active toggles (not on every PUT). Public /gtk/v1/pricing is read-only. **7th cumulative money-flow integrity verification across this PKG chain.**

## Known caveats

1. **Public /gtk/v1/pricing has no rate-limit middleware** — low-risk (it's a cheap query) but worth instrumenting if it becomes hot. NIT from review.
2. **gtk_billing_config is read on every WriteUsageLog call without cache** — design choice for immediate markup propagation. Admin should avoid mass updates during peak.
3. **PKG-INVITE-REDEEM still deferred** (admin endpoints for invitation/redeem absent — by design per founder 2026-05-14).
4. **L3 服务目录 admin CRUD (ADMIN-L2-2) deferred** — Stream C OpenClaw reshape risk + founder 5/10 priority memo (L1+L2 robust first).

## Rollback procedure

```
bash bin/v22-deploy.sh rollback
```
Rolls back to `greentokey-coai:v0.30.0-phase-1-excision`. Image still on VPS. docker-compose.yml backup at `/opt/greentokey/docker-compose.yml.bak.v1.0.0004`. Source backup at `coai-source.bak.v1.0.0004`.

**Schema rollback**: NOT needed. The +7 display columns on gtk_provider_pricing are nullable; rolling back to v0.30.0 with those columns present is safe (older code ignores them). seedDisplayPricing's UPDATE-first-then-INSERT-IGNORE pattern leaves no orphan rows.

## Next actions

- **Founder**: open browser → /admin/gtk to verify:
  - 套餐定价 tab still renders (3 sections cleanly, no regression)
  - **Left rail icon column is gone** (ToolBar excise visual fix) ← key UX validation
  - **No right-side empty space** (layout撑满) ← key UX validation
  - Optional: test new "在公开 Pricing 页显示" toggle in provider-pricing add-row form
- **Founder**: open /pricing public page to confirm 8 models render same as before (visual continuity).
- **24h watch**: any 5xx spike or panic in `docker compose logs coai` → rollback. Otherwise stable.
- **No new dev work tonight** — founder's choice to seal v0.31.0 ship + sleep.

## Coords

- Live: https://api.greentokey.com — landing + admin (/admin/gtk) + public pricing (/pricing)
- Image: `greentokey-coai:v0.31.0-pricing-dynamic`
- VPS: `/opt/greentokey/coai-source` (rsynced); `/opt/greentokey/docker-compose.yml` (tagged)
- Branch HEAD: `ec659f9`
- Public pricing API: https://api.greentokey.com/api/gtk/v1/pricing
