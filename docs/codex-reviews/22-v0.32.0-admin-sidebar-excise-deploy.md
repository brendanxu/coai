# v0.32.0-admin-sidebar-excise — Deploy Report

**Date**: 2026-05-17 15:21 SGT (15:13 UTC)
**Image**: `greentokey-coai:v0.32.0-admin-sidebar-excise` (78.7MB, 2026-05-17 07:20:55 UTC build)
**Previous prod**: `v0.31.0-pricing-dynamic`
**PKG**: PKG-A-1 (Admin Sidebar Excise — phase 1 of unified admin hub migration)

## Scope

Single PKG bundle:
- **Excise CoAI native admin sidebar** — MenuBar.tsx now returns `null` instead of rendering 6 menu items
- All admin surface now consolidated to `/admin/gtk` (GtkAdmin unified hub, 1489 LOC, 7 tabs)
- `/admin` root already redirected to `/admin/gtk` (pre-existing router behavior)

## Branch / Commits

- Feature branch: `feat/v0.32-admin-sidebar-excise`
  - `3966cbc` refactor(admin): MenuBar.tsx 6 项 sidebar 收口 — return empty div
  - `2ec9cbc` refactor(admin): MenuBar return null — fix codex P2 sidebar reservation
- Merged to `feat/v0.22-token-launch` via `--no-ff`
  - Merge: `(implicit merge commit)`
  - Tag bump: `61b4d73` chore(deploy): bump v22-deploy.sh tags v0.31 → v0.32

## Code Review

- **Reviewer**: Codex CLI 0.125.0 (`codex review --commit 3966cbc`)
- **Findings**: 1 × P2 (sidebar reservation), fixed in `2ec9cbc`
- **Re-review**: Skipped (fix was 1-to-1 with codex recommendation)
- **Report**: `docs/codex-reviews/21-v0.32-admin-sidebar-excise-review.md`

## Deploy steps

| Step | Status | Notes |
|---|---|---|
| 1. `git checkout feat/v0.22-token-launch` | ✅ | Already at tip |
| 2. `git merge --no-ff feat/v0.32-admin-sidebar-excise` | ✅ | 1 file, +3/-86 net |
| 3. `sed` bump v22-deploy.sh tags | ✅ | PREV `v0.31.0-pricing-dynamic` → NEW `v0.32.0-admin-sidebar-excise` |
| 4. `git push origin feat/v0.22-token-launch` | ✅ | `9d2d6de..61b4d73` |
| 5. `bin/v22-deploy.sh rsync` | ✅ | Backup + rsync to VPS, ~30s |
| 6. `bin/v22-deploy.sh build` | ✅ | Docker build on VPS, 78.7MB |
| 7. `bin/v22-deploy.sh up` | ✅ | Container recreated, 0 panic, services healthy |
| 8. `bin/v22-deploy.sh smoke` | ✅ | All endpoints 200 / expected; token-99 plan row intact |

## Smoke results

| Check | Result |
|---|---|
| `/token-plans` JS bundle CTA i18n keys | ✅ Present |
| `/api/payment/checkout?plan_code=token-99` | HTTP 200 (auth envelope) |
| `/api/payment/hupijiao/checkout?plan_code=token-99` | HTTP 200 (auth envelope) |
| Hupijiao callback (no sig) | HTTP 500 ✅ (merchant pre-signup state, expected) |
| `/api/gtk/v1/services` | HTTP 200 |
| `/api/gtk/v1/pool` | HTTP 200 |
| `gtk_plan` row `token-99` (id=5, ¥99 / 5000 quota) | ✅ Intact |

## Visual verification (founder)

**MUST CHECK** before marking PKG-A-1 done:
1. Open `https://api.greentokey.com/admin` — should redirect to `/admin/gtk`
2. On `/admin/gtk` — verify NO left sidebar visible (GtkAdmin full-width)
3. Manually navigate to `/admin/users` / `/admin/orders` — verify NO blank reserved sidebar space (codex P2 fix verification)

## Risk / Rollback

- **Risk**: low — single-file React/TS refactor, no schema change, no API change, no payment logic touched
- **Rollback**: `bash bin/v22-deploy.sh rollback` (rolls to v0.31.0-pricing-dynamic in ~30s)
- **Backup**: `/opt/greentokey/coai-source.bak.v1.0.<HHMM>/` + `/opt/greentokey/docker-compose.yml.bak.v1.0.<HHMM>/`

## Next

- PKG-A-1 phase 2 (deferred): delete orphan files (License.tsx, Notification.tsx, UserRouting.tsx) + their router routes
- PKG-A-3 (next sprint): 令牌管理 as GtkAdmin 8th tab (12-15h) — founder priority 1
- Per CLAUDE.md MONITOR step: `/canary` 24h soak watch recommended
