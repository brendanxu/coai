# v0.30.0-phase-1-excision-admin-l2-1 — PROD DEPLOY ✅

**Deployed**: 2026-05-15 19:26 SGT (11:26 UTC)
**Image**: `greentokey-coai:v0.30.0-phase-1-excision` (78.5 MB, ~19% smaller than v0.29.x)
**Branch**: `feat/v0.22-token-launch` HEAD `7df654d`
**Previous prod**: `v0.29.5-admin-polish` (rollback target)

## What shipped (cumulative since v0.29.5)

**Phase 1 — CoAI chat-stack excision** (~20,659 LOC purged, 9 PKGs, 22 commits):

| PKG | Commit range |
|---|---|
| EXCISE-1a 前端纯删 | `9d420d7` |
| EXCISE-1b AppProvider 解耦 | `55ab5d2` |
| EXCISE-1c-prep 前端独立解耦 | `891a830` |
| EXCISE-1c-gate System+Account 解耦 | `3af346b → 091fda6` |
| (LOW-1 fixup /wallet → /pricing) | `7eeafd4` |
| EXCISE-1c-sweep chat 树清空 | `48640b8 → c54947b` |
| EXCISE-2a main.go 解钩 | `674308c` |
| EXCISE-3 Path B bulldoze | `af082e0 → 127de99` |
| EXCISE-4 admin/ 剪枝 | `d27157a → 5a1d1e1` |
| EXCISE-2b delete adapter/ | `b464b36` |

**Phase 2 — ADMIN-L2-1** (~2,191 LOC new, 4 commits):

| Commit | Content |
|---|---|
| `031c04b` | plans/billing-config/provider-pricing handlers + 9 endpoints |
| `a5929c1` | 34 handler unit tests + auth gates |
| `f884c4a` | GtkAdmin pricing tab UI (3 sections, RatiosTab pattern) + 17 i18n keys (cn/en) |
| `71455b0` | LOW-1 fix — markup_multiplier reject NaN/Inf |

**Deploy infrastructure fixes** (2 commits):

| Commit | Content |
|---|---|
| `272d806` | bin/v22-deploy.sh PREV_TAG=v0.29.5-admin-polish, NEW_TAG=v0.30.0-phase-1-excision |
| `7df654d` | Dockerfile.split drop addition/article/template.docx COPY (file deleted in EXCISE-3) |

**Total**: 28 commits / net -18,468 LOC (+2,191 new ADMIN-L2-1, -20,659 chat-stack purge).

## Deploy steps executed

1. ✅ **Preflight** (`bin/v22-deploy.sh preflight`) — cross-grep introduce-and-forget defender, 2 patterns checked (next_query_param + greentokey_session_id), all producers have consumers.
2. ✅ **rsync** (`bin/v22-deploy.sh rsync`) — backed up old coai-source to `coai-source.bak.v1.0.1922`; rsynced Mac → VPS with `--delete`; verified payment/hupijiao_*.go + plans/lookup.go on VPS.
3. ✅ **Docker build** (`bin/v22-deploy.sh build`) — Dockerfile.split Go-only build on VPS, exit clean, image `v0.30.0-phase-1-excision` 78.5MB.
4. ✅ **Compose up** (`bin/v22-deploy.sh up`) — `docker-compose.yml` image tag bumped v0.29.5-admin-polish → v0.30.0-phase-1-excision; backup saved as `.bak.v1.0.1924`; `docker compose up -d coai` recreated container; container started healthy in <10s; logs clean (mysql + redis connected; 4 users found, skip root creation; newapi.DrainPendingProvisionsForever started; billing cron started, no plans to expire; SEO + PWA manifest applied; static files serving from ~/app/dist; **0 migration panics**).
5. ✅ **Smoke tests** (`bin/v22-deploy.sh smoke`):
   - /token-plans bundle contains dual-rail CTA i18n keys ✓
   - /api/payment/checkout HTTP 200 (auth-gated envelope) ✓
   - /api/payment/hupijiao/checkout HTTP 200 (auth-gated envelope) ✓
   - Hupijiao callback HTTP 500 (merchant not configured — expected) ✓
   - /api/gtk/v1/services HTTP 200 ✓
   - /api/gtk/v1/pool HTTP 200 ✓
   - token-99 plan row exists in gtk_plan (id=5, price_cents=9900, quota_grant=5000) ✓
6. ✅ **ADMIN-L2-1 endpoint smoke** (additional):
   - /api/gtk/v1/admin/plans → HTTP 200 + `{"error":"user not found","status":false}` (CoAI-style auth gate working) ✓
   - /api/gtk/v1/admin/billing-config → HTTP 200 + `{"error":"user not found","status":false}` ✓
   - /api/gtk/v1/admin/provider-pricing → HTTP 200 + `{"error":"user not found","status":false}` ✓

## Pre-deploy fixes

**Dockerfile.split COPY bug** caught during prep: line 46 referenced `addition/article/template.docx` but `addition/` was deleted in EXCISE-3 commit `af082e0`. Would have failed docker build. Removed in commit `7df654d` before rsync.

## Money-flow integrity

`git diff v0.29.5-admin-polish..v0.30.0-phase-1-excision -- payment/ commerce/ service/ newapi/usage.go newapi/router.go plans/lookup.go` shows **only additive changes** in newapi/ (new admin handlers added — no edits to existing money-path). Pre-existing money endpoints (LS webhook, Hupijiao webhook, usage write, cost ledger compute) all untouched. 5th verification of money-flow integrity across this PKG chain.

## Known caveats

1. **i18n ja/ru conflict markers in source** — pre-existing TECHDEBT since v0.21 cleanup merge (`773bc80`, 2026-05-12). Files on disk have `<<<<<<<` markers but vite bundle was successfully built (greentokey hard-codes "cn" so ja/ru never loaded at runtime). LOW-1 commit landed via `git commit --only` bypass. Future PKG-I18N-EXPAND will resolve as part of overseas wedge work.
2. **3 pre-existing test failures unchanged**: commerce/cost_ledger ×2 + service/seed ×1. Listed in DEV-PLAN.md §0 as deferred.
3. **vet warns down from 3 to 2**: adapter/skylark/chat.go warn died with adapter/ delete; utils/image.go ×2 remain.
4. **PKG-PRICING-DYNAMIC deferred**: Pricing.tsx still shows hardcoded MODEL_ROWS; admin can't yet change customer-facing prices. Backlog in DEV-PLAN §2.4 (4-6h dev).

## Rollback procedure

If anything goes wrong in next 24h:
```
bash bin/v22-deploy.sh rollback
```
Rolls back to `greentokey-coai:v0.29.5-admin-polish`. Image is still on VPS. docker-compose.yml backup at `/opt/greentokey/docker-compose.yml.bak.v1.0.1924`. coai-source backup at `coai-source.bak.v1.0.1922`.

DB schema is unchanged from v0.29.5 (no ALTER in this deploy — PKG-PRICING-DYNAMIC defers schema changes). Rollback is purely image swap; no data migration needed.

## Next actions

- **Founder**: run `/canary` from this Mac to start 24h monitoring loop (alerts on anomalies).
- **Founder**: log into GTK admin → 套餐定价 tab to verify UI loads correctly (3 sections render, "新增套餐" / "保存修改" / "停用" buttons present).
- **Founder**: optional smoke — try editing markup_multiplier to 1.4 then back to 1.3 (verifies UPSERT + audit gate).
- **24h watch**: any 5xx spike or panic in `docker compose logs coai` → rollback. Otherwise after 24h, declare stable and begin PKG-ADMIN-L2-2 or PKG-PRICING-DYNAMIC planning.

## Coords

- Live: https://api.greentokey.com — landing + admin (/admin/gtk)
- Image: `greentokey-coai:v0.30.0-phase-1-excision`
- VPS path: `/opt/greentokey/coai-source` (synced); `/opt/greentokey/docker-compose.yml` (tagged)
- Branch HEAD: `7df654d`
