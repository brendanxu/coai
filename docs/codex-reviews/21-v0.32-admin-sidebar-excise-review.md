The patch removes the menu items but leaves the styled sidebar container in place, causing blank space or a full-width empty panel on existing admin shell routes. This is a functional layout regression for those routes.

Review comment:

- [P2] Stop rendering a styled empty sidebar — /Users/brendanxu/tanaxu/greentokey/coai-v0.22-token-checkout/app/src/components/admin/MenuBar.tsx:9-9
  When an admin opens any remaining `/admin/*` shell route such as `/admin/users` or `/admin/orders`, this empty element still gets the `.admin-menu` styles, which reserve 12rem by default on desktop (`menu.open` starts true) and 4.25rem/full width on mobile when toggled. That leaves a blank sidebar/panel taking over layout even though the sidebar was excised; render `null` or remove the sidebar styling instead of keeping the `admin-menu` placeholder.
