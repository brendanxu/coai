# v0.33.0-admin-orphan-cleanup — Deploy Report

**Date**: 2026-05-17 18:32 SGT (10:32 UTC)
**Image**: `greentokey-coai:v0.33.0-admin-orphan-cleanup` (78.7MB)
**Previous prod**: `v0.32.0-admin-sidebar-excise`
**PKG**: PKG-A-1 phase 2 (admin orphan files + dead routes cleanup)

## Scope

Single PKG bundle:
- **Remove orphan admin files** that were already disconnected from menu:
  - `License.tsx` (175 LOC) — CoAI-native license purchase stub, was reused across 4 dead routes
  - `Notification.tsx` (6 LOC) — empty placeholder
  - `UserRouting.tsx` (339 LOC) — PKG-4 functionality already merged into GtkAdmin/channels tab
- **Remove 5 dead router entries**: `/admin/license`, `/admin/warmup`, `/admin/record`, `/admin/pay`, `/admin/user-routing`
- **Preserve** `Mansu.tsx` + `/admin/mansu` route (Gate 1 民宿 concierge, founder direct-URL use)
- **Preserve** `api/userRouting.ts` (still actively imported by GtkAdmin.tsx)

## Branch / Commits

- Feature branch: `feat/v0.32-admin-orphan-cleanup` (3 atomic commits)
  - `386f9e8` chore(admin): remove License.tsx + 4 stub routes
  - `eece460` chore(admin): remove Notification.tsx placeholder
  - `d25c9e8` chore(admin): remove UserRouting.tsx
- Merged to `feat/v0.22-token-launch` via `--no-ff`
- Tag bump: `95f94e0` chore(deploy): bump v22-deploy.sh tags v0.32 → v0.33

## Code Review

- **Reviewer**: Codex CLI 0.125.0 (`codex review --base feat/v0.22-token-launch`)
- **Verdict**: 0 finding
- **Quote**: "The deleted admin pages are no longer imported or linked from the current frontend, and the remaining routes build successfully. I did not find a discrete regression introduced by these changes."
- **Report**: `docs/codex-reviews/23-v0.32-admin-orphan-cleanup-review.md`

## Deploy steps

| Step | Status | Notes |
|---|---|---|
| 1. `git checkout feat/v0.22-token-launch` | ✅ | Already at tip |
| 2. `git merge --no-ff feat/v0.32-admin-orphan-cleanup` | ✅ | 4 files, +1/-573 net |
| 3. `sed` bump v22-deploy.sh tags | ✅ | PREV v0.32 → NEW v0.33 |
| 4. `git push origin feat/v0.22-token-launch` | ✅ | `06470ac..95f94e0` |
| 5. `bin/v22-deploy.sh rsync` | ✅ | Backup + rsync to VPS |
| 6. `bin/v22-deploy.sh build` | ✅ | Docker build 78.7MB, 1.3s manifest export |
| 7. `bin/v22-deploy.sh up` | ✅ | Container recreated, 0 panic |
| 8. `bin/v22-deploy.sh smoke` | ✅ | All endpoints expected status |

## Smoke results

| Check | Result |
|---|---|
| `/token-plans` JS bundle CTA i18n keys | ✅ Present |
| `/api/payment/checkout?plan_code=token-99` | HTTP 200 (auth envelope) |
| `/api/payment/hupijiao/checkout?plan_code=token-99` | HTTP 200 (auth envelope) |
| Hupijiao callback (no sig) | HTTP 500 ✅ (merchant pre-signup, expected) |
| `/api/gtk/v1/services` | HTTP 200 |
| `/api/gtk/v1/pool` | HTTP 200 |
| `gtk_plan` row `token-99` (id=5, ¥99 / 5000 quota) | ✅ Intact |

## Visual verification (founder)

Manual checks after deploy:
1. Open `https://api.greentokey.com/admin/license` → should 404 / fallback (not crash)
2. Same for `/admin/warmup` / `/admin/record` / `/admin/pay` / `/admin/user-routing`
3. `/admin/gtk` still works (GtkAdmin unified hub)
4. `/admin/mansu` still works (Mansu preserved)

## Risk / Rollback

- **Risk**: low — pure deletion, no schema change, no API change, no payment touched
- **Rollback**: `bash bin/v22-deploy.sh rollback` (rolls to v0.32.0-admin-sidebar-excise in ~30s)
- **Backup**: `/opt/greentokey/coai-source.bak.v1.0.<HHMM>/` + `docker-compose.yml.bak.v1.0.<HHMM>/`

## Cumulative LOC reduction (Sprint A-1)

- Phase 1 (v0.32.0): MenuBar.tsx 12 → 7 LOC (-78 LOC net)
- Phase 2 (v0.33.0): orphan files + dead routes (-573 LOC net)
- **Sprint A-1 total: -651 LOC** of CoAI legacy admin surface

## Next

- PKG-A-3 (令牌管理 / Personal Access Tokens) — designed in `.planning/research/2026-05-17-pkg-a3-token-management-design.md`
- Pending founder decision: 单 token (A, 10-12h) vs 多 token (B, 14-16h)
- After decision: `/gsd-plan-phase PKG-A-3` → execute → deploy v0.34.0
- Per CLAUDE.md MONITOR: `/canary` 24h soak watch recommended
