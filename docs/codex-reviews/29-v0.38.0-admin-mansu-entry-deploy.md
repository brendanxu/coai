# v0.38.0-admin-mansu-entry — Deploy Report

**Date**: 2026-05-31
**Image**: greentokey-coai:v0.38.0-admin-mansu-entry
**Previous**: v0.37.2-pricing-dedupe
**Branch**: feat/v0.22-token-launch
**Merge commit**: 4163047
**PKG**: admin mansu topbar entry + orphan admin cleanup

## Scope
- Task A: add top-bar `民宿工作台` entry in `/admin/gtk`, linking to `/admin/mansu`.
- Task B: remove 12 orphaned admin files and dead CoAI admin payment helpers.
- Keep unified admin hub invariant: `/admin` redirects to `/admin/gtk`; `/admin/mansu` remains an admin-gated workspace reached from the hub.
- Keep NewAPI isolation invariant: NewAPI remains Docker-internal only; no public route or host port added.
- No payment, commerce, service, billing, or migration behavior changed beyond deleting unused frontend helper functions.

## Commits
- a0af991 feat(admin): add 民宿工作台 entry button in unified /admin/gtk top bar
- 1a714aa chore(admin): remove orphaned dead admin code (-2483 LOC)
- 4163047 Merge branch 'chore/admin-cleanup-deadcode' into feat/v0.22-token-launch

## Review
- Confirmed the button is inside `gtk-topbar-right`, before the `系统正常` status indicator, using `<Link to="/admin/mansu">` and class `btn btn-ghost btn-sm`.
- Confirmed no sidebar mansu entry was added.
- Confirmed all 12 target files are absent.
- Exact import trace for the deleted admin files returned no matches.
- Confirmed retained files still have live references: `admin/api/system.ts`, `admin/api/info.ts`, `admin/channel.ts`, `admin/types.ts`, and `admin/charge.ts`.

## Verification
- `npx tsc --noEmit`: `EXIT:0`
- `npm run build`: `EXIT:0`; chunks include `dist/assets/Mansu.47bc84f4.js` and `dist/assets/GtkAdmin.1e636374.js`.
- `npm run test`: `EXIT:0`; 3 files passed, 47 tests passed.
- Known pre-existing test noise: localhost `:3000` connection refusals for mocked app bootstrap calls and `router.tsx:322` React warning.

## Deploy
- Dry-run: `bin/deploy.sh coai --coai-version v0.38.0-admin-mansu-entry --dry-run` -> `EXIT:0`.
- Production deploy: `bin/deploy.sh coai --coai-version v0.38.0-admin-mansu-entry` -> image built and `greentokey-coai` recreated.
- Pre-deploy backup: `/opt/greentokey/data/backups/greentokey-20260531-032846.sql.gz`.
- Post-deploy smoke passed for `/token-plans`, `/api/gtk/v1/services`, and `/api/gtk/v1/pool`.
- The built-in 60m canary later exited with `curl (35) LibreSSL SSL_connect: SSL_ERROR_SYSCALL`; production stayed healthy, so this was treated as a transient local TLS/network failure rather than an app failure.
- Follow-up diagnosis: `greentokey-coai` was still up on the new image after ~59 minutes; 5 manual `/services` + `/pool` loops returned 200; a follow-up `bin/canary.sh 5` passed with `EXIT:0`.

## Smoke
- `https://greentokey.com/admin`: 200, title `greentokey · 民宿主的小红书运营官`.
- `https://greentokey.com/admin/gtk`: 200, title `greentokey · 民宿主的小红书运营官`.
- `https://api.greentokey.com/admin`: 200, title `greentokey · 民宿主的小红书运营官`.
- `https://api.greentokey.com/admin/gtk`: 200, title `greentokey · 民宿主的小红书运营官`.
- Public `:3000` checks against `greentokey.com` and `api.greentokey.com`: `http_code=000`, curl exit 28 timeout.
- Remote `docker compose ps`: `greentokey-newapi` exposes `3000/tcp` internally only; no published host port.

## Risk / Rollback
- Risk: low. UI entry addition plus dead-code removal; no runtime schema or billing logic changes.
- Rollback: restore previous compose image tag `greentokey-coai:v0.37.2-pricing-dedupe` and `docker compose up -d coai`.

## Notes
- Original 60m canary did not finish cleanly because of one local curl TLS error. No server-side app crash, container restart, or NewAPI exposure regression was observed.
